# catalog-signer

Owner: offline signing operators.

This directory owns offline candidate revalidation and signature-fragment production. It must not build or mutate candidates, access a network, deploy bytes, or expose signing material.
