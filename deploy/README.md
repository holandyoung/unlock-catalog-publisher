# Deployment boundary

Owner: release operators.

This directory is reserved for publication-side documentation and integration added by later tasks. It must not contain live credentials, signing material, production resource state, or platform runtime dependencies.
